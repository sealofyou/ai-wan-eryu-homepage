# AI玩尔玉三维桌面主页：双模型共同实现任务

## 任务目标

基于当前 Astro 项目，实现一版仅面向电脑端的可交互个人主页原型。主页不是普通网页套上桌面皮肤，而是一张真正可玩的卡通三维电脑桌面：桌面物件本身承担导航、内容展示和互动功能。

本任务用于 Codex 与 Kimi K3 的同条件对照。必须独立完成，不读取另一个实现目录，不复用另一个版本的代码或截图。

## 必读参考图

按以下优先级读取 `reference/`：

1. `desktop-homepage-composition-approved-v1.png`
   - 用户已经确认的最终构图，是视觉与物件位置的最高优先级依据。
2. `real-desk-photo.jpg`
   - 真实桌面的空间来源，用于理解机箱、竖屏、横屏、桌垫和键盘的真实关系。
3. `main-avatar-v1.png`
   - 中间主屏个人介绍使用的人物形象参考。
4. `q-avatar-v1.png`
   - 机箱顶部 Q 版摆件参考，只能作为小型彩蛋，不能成为主视觉大头像。
5. `eryu-wordmark-board-v1.png`
   - 设备上的 `Eryu` 字标和 E 形符号参考。

参考图只用于指导建模、材质、构图和屏幕内容。禁止把完整构图图设置为全屏背景，再用透明热点伪装交互。

## 已确认构图

- 视角为正面略微俯视的近景，主屏足够大，访问者可以轻松看清内容。
- 左侧是黑色机箱，机箱顶放一个 Q 版尔玉小摆件。
- 机箱正面保留一张黄色便利贴，用于每日格言。
- 机箱右侧是竖向副屏，用于活动、分享和做过的事情。
- 中间是横向主屏，用于个人介绍、当前状态和被选中的活动详情。
- 主屏下方是带有 `Eryu` 标识的横向音箱。
- 桌面前景是一整张浅色大鼠标垫，完整键盘和鼠标都放在同一张垫子上。
- 不出现独立黑色鼠标垫、抽屉键盘、小木键盘、外露线材、书堆、水瓶、纸巾盒、杯子、保温杯或右侧笔记本。
- 右侧保留台灯和黑色洞洞板。洞洞板上只有少量 Eryu 风格徽章占位，不编造活动、公司、奖项或参与经历。
- 整体为精致玩具模型感的三维卡通场景：温暖木色、炭黑设备、浅色桌垫、少量低饱和绿色与彩色屏幕内容。

## 必须实现的交互

### 1. 三维场景

- 使用 Three.js 建立真实场景、相机、灯光、网格、材质与可点击物件。
- 物件必须有空间层次、阴影、圆角倒角或等价的玩具模型质感。
- 鼠标移动时允许极轻微镜头视差，幅度不能影响阅读。
- 台灯和显示器产生温暖灯光与屏幕光，但不做霓虹、赛博朋克或全屏泛光。
- 页面加载时允许一次短暂的镜头进入或设备亮起动画。

### 2. 中间主屏

默认显示自然、克制的中文个人介绍与当前状态：

- 品牌：`AI玩尔玉`
- 次级标识：`Eryu`
- 简介：`把 AI 接进真实工作流，做成能运行、能验证、也能继续交接的系统。`
- 当前状态区只放 2 至 3 条简短占位内容，不出现营销话术、虚构数据和客户案例。

主屏内容应以 CanvasTexture、CSS3DRenderer 或等价的真实屏幕纹理方式存在于显示器内部。禁止把普通 HTML 卡片漂浮在显示器外侧。

### 3. 左侧活动屏

左屏至少提供三个可点击入口：

- `07.04 分享`
- `07.18 活动`
- `最近在做`

入口以图片缩略图、日期和短标题为主，不堆大量文字。点击后：

- 中间主屏平滑切换为对应详情。
- 详情可以使用真实仓库中已有的公开图片或克制的占位画面。
- 缺少资料的位置直接写 `内容整理中`，不得编造活动规模、演讲反馈或参与成绩。
- 必须提供自然的返回主页方式，返回控件只能出现在主屏内部。

### 4. 物理鼠标与屏幕光标

- 桌面上的实体鼠标可以在鼠标垫范围内拖动。
- 拖动实体鼠标时，中间显示器内的光标必须同步移动，方向与比例合理。
- 鼠标不能被拖出鼠标垫，松手后保留当前位置。
- 点击和拖动需要区分，不能因为一次点击导致鼠标跳动。

### 5. 键盘与鼠标垫

- 键盘可以在鼠标垫允许范围内拖动，并具有轻微抬起、阴影变化或回弹反馈。
- 鼠标垫需要支持简单画画或书写：用户能留下连续笔迹，也能清空。
- 鼠标垫需要支持文字留言输入。可以在聚焦垫子或打开垫子上的小型工具后调用键盘输入，但控件必须属于桌面物件本身，不能变成页面外部工具栏。
- 书写模式、留言模式与物件拖动不能相互误触。

### 6. 便利贴与徽章墙

- 点击机箱上的便利贴，显示或更换一条简短每日格言。初始可使用：`保持好奇，继续动手。`
- 洞洞板上的徽章可点击。当前只需要实现一个示例彩蛋：点击后让主屏出现简短故事占位或 Q 版人物反馈。
- 示例内容必须明确是未填充状态，不得冒充真实经历。
- 徽章数据结构要便于未来追加用户提供的 Logo、贴纸、时间和故事。

## 内容与文案边界

- 当前只搭架子，不追求填满作品、文章和活动数据。
- 不恢复文章导航、合作页、截图隐私打码器或中转站内容。
- 不出现“使用 Q 版降低理解门槛”“这是一个工作台”“点击这里探索”等解释设计过程的元话语。
- 不出现 `SYSTEM`、终端 HUD、AR 标记、底部黑色说明栏、外部导航栏或悬浮营销按钮。
- 不公开服务器 IP、凭据、本地路径、内部项目细节或未经确认的合作信息。
- 所有可见文本以中文为主，英文只用于 `Eryu` 等品牌标识。

## 禁止方向

- 普通营销落地页、简历长页或卡片墙。
- 用 CSS 画一个扁平桌面，再把网页模块塞在上面。
- 直接使用批准构图图作为整页背景图或视频背景。
- GBA、掌机、手机壳、桌面关卡、移动端触控板或任何手机版方案。
- 霓虹赛博朋克、蓝紫渐变、粒子宇宙、全屏终端、科幻 HUD、AR 扫描线。
- 大量连续自动动画、镜头自动旋转、影响阅读的景深或摇晃。
- 为了技术炫耀牺牲主屏可读性。

## 技术约束

- 保留 Astro 作为站点框架。
- 使用 Three.js 作为三维核心，不手写替代性的伪 3D 引擎。
- 可以自行决定使用原生 TypeScript、Astro island 或 React，但不要引入大型 UI 组件库。
- 允许新增完成任务所需的 `three`、类型包和测试依赖。
- 代码应按场景、物件、内容状态和交互职责拆分，避免把全部逻辑塞进一个超大文件。
- 只修改当前实现目录，不部署、不覆盖原站、不访问另一个对照版本。
- 当前只验收桌面视口。移动端可显示简单的“桌面版体验准备中”静态提示，但不能投入设计或实现时间。

## 可访问性与降级

- 所有关键点击物件需要有可识别的 DOM 语义或等价键盘入口。
- 支持 `prefers-reduced-motion`，降低镜头和闲置动画。
- WebGL 初始化失败时，显示一张构图预览和简短说明，不能出现空白页。
- 页面必须有明确标题和描述信息。

## 验收要求

必须完成并记录：

1. `npm install` 或 `npm ci` 成功。
2. `npm run check` 通过。
3. `npm test` 通过。
4. `npm run build` 通过。
5. 启动本地开发服务器。
6. 使用 Playwright 或等价真实浏览器，在 `1440x900` 与 `1920x1080` 截图。
7. 检查 WebGL canvas 非空，主屏、左屏、机箱、键盘、鼠标、桌垫、台灯和徽章墙均在画面内。
8. 自动或手动验证：左屏切换主屏、返回主页、实体鼠标拖动与屏幕光标同步、键盘拖动、鼠标垫笔迹、留言输入、便利贴和徽章点击。
9. 截图保存到 `output/screenshots/`。
10. 最终说明写入 `IMPLEMENTATION_RESULT.md`，包括运行地址、改动文件、已完成交互、测试结果和已知不足。

## 评判重点

按以下权重比较两个版本：

| 维度 | 权重 |
| --- | ---: |
| 是否让人第一眼觉得好玩、有意思、想点击 | 25% |
| 与批准构图的视觉接近程度 | 20% |
| 三维质感、空间层次和物件细节 | 15% |
| 主屏内容可读性与左屏切换体验 | 15% |
| 键鼠、鼠标垫、便利贴和徽章交互完成度 | 15% |
| 性能、稳定性、降级与代码可维护性 | 10% |

最终目标不是做最多功能，而是做出一张真正属于 AI玩尔玉、可以被访客把玩的电脑桌面。
