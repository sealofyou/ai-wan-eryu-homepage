# 独立实现任务

你是本次对照实验中的独立前端实现者。

请先完整阅读当前目录的 `AGENTS.md`、`IMPLEMENTATION_BRIEF.md`，并查看 `reference/` 中全部图片，然后直接完成实现，不要只输出分析、设计方案或待办清单。

要求：

1. 严格实现 `IMPLEMENTATION_BRIEF.md`，保持桌面端范围。
2. 独立做技术与视觉判断，不读取当前目录之外的另一个模型实现。
3. 可以安装必要依赖，必须使用 Three.js 构建真实可交互场景。
4. 完成编码、测试、构建、真实浏览器截图和交互验证。
5. 修复发现的问题，直到关键验收项通过或已明确记录无法解决的阻塞。
6. 将截图放入 `output/screenshots/`，将完整结果写入 `IMPLEMENTATION_RESULT.md`。
7. 最后启动本地开发服务器，使用当前目录可用的独立端口，并在结果文档中记录 URL。

不要部署，不要覆盖原主页，不要处理手机版。现在开始并持续完成整个任务。
