# 对照实现规则

1. 先完整阅读 `IMPLEMENTATION_BRIEF.md` 与 `reference/` 中全部参考图。
2. 只在当前目录实现，不读取或修改兄弟目录中的另一个模型版本。
3. 独立完成设计决策、编码、测试、截图与结果报告。
4. 不部署，不覆盖原站，不处理手机版。
5. 不把批准构图图直接作为全屏背景图或热点图使用。
6. 完成后保留本地开发服务器地址，并写入 `IMPLEMENTATION_RESULT.md`。
